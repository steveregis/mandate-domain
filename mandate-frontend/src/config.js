// config.js

// If there's an environment variable called REACT_APP_BACKEND_URL, use that.
// Otherwise, default to http://localhost:8080
export const BACKEND_URL = process.env.REACT_APP_BACKEND_URL || 'http://localhost:8080';
